\chapter{Dependency Graphs}
\label{app:dep-graphs}

\depgraph[.55]{GetOperand}{Dependency graph of \texttt{GetOperand}, which resolves a source operand to a DAG index by dispatching on operand type (register, memory, immediate).}

\depgraph[.55]{SetOperand}{Dependency graph of \texttt{SetOperand}, which writes a DAG index to a destination operand, handling register aliasing and memory stores.}

\depgraph[.6]{GetOperand_R}{Dependency graph of \texttt{GetOperand\_R}, the correspondence lemma relating symbolic \texttt{GetOperand} to concrete operand evaluation.}

\depgraph[.4]{R_SetOperand}{Dependency graph of \texttt{R\_SetOperand}, the correspondence lemma relating symbolic \texttt{SetOperand} to concrete writing of registers or memory addresses.}