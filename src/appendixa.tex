\chapter{Dependency Graphs}
\label{app:dep-graphs}

\depgraph[.5]{GetOperand}{Dependency graph of \texttt{GetOperand}, which resolves a source operand to a DAG index by dispatching on operand type (register, memory, immediate).}

\depgraph[.45]{SetOperand}{Dependency graph of \texttt{SetOperand}, which writes a DAG index to a destination operand, handling register aliasing and memory stores.}

\depgraph[.45]{GetOperand_R}{Dependency graph of \texttt{GetOperand\_R}, the correspondence lemma relating symbolic \texttt{GetOperand} to concrete operand evaluation.}
